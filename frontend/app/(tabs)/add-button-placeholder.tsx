import React from 'react';

export default function AddButtonPlaceholderScreen() {
    // This screen doesn't render anything visible,
    // it is used as a placeholder for the add button, 
    // so that the button can be positioned in the tab bar.
    return null;
  }